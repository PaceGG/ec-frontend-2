import { createTheme } from "@mui/material/styles";

export const darkTheme = createTheme({
  palette: {
    mode: "dark",
    primary: { main: "#0b79d0" },
    success: { main: "#669868" },
    error: { main: "#c25050" },
  },
});
